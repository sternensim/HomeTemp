version: '3.8'

services:
  # Home Assistant Container
  homeassistant:
    image: homeassistant/home-assistant:latest
    container_name: homeassistant
    restart: unless-stopped
    privileged: true
    environment:
      - TZ=Europe/Berlin  # Change to your timezone
    ports:
      - "8123:8123"       # Home Assistant web UI
    volumes:
      - ./home-assistant/config:/config
      - /etc/localtime:/etc/localtime:ro
      - /run/dbus:/run/dbus:ro
    networks:
      - homeautomation
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8123"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ESPHome Container (manages ESP32 nodes)
  esphome:
    image: esphome/esphome:latest
    container_name: esphome
    restart: unless-stopped
    environment:
      - TZ=Europe/Berlin  # Change to your timezone
    ports:
      - "6052:6052"       # ESPHome web dashboard
    volumes:
      - ./esphome:/config
      - /etc/localtime:/etc/localtime:ro
    networks:
      - homeautomation
    depends_on:
      - homeassistant
    # Uncomment for USB device access (programming ESP32)
    # devices:
    #   - /dev/ttyUSB0:/dev/ttyUSB0    # First USB device
    #   - /dev/ttyUSB1:/dev/ttyUSB1    # Second USB device (if needed)
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6052"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Portainer (container management UI)
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    ports:
      - "9000:9000"       # Portainer web UI
      - "8000:8000"       # Portainer edge agent
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./portainer/data:/data
    networks:
      - homeautomation

networks:
  homeautomation:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

# Quick start commands:
# docker-compose up -d              → Start all services
# docker-compose down               → Stop all services
# docker-compose logs -f            → View logs (all services)
# docker-compose logs -f homeassistant → View HA logs only
# docker-compose logs -f esphome    → View ESPHome logs only
# docker-compose ps                 → Check service status
